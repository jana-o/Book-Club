<div>Test</div>
<div class="row">
    <div class="col-6">
        <img class="card-img-top" src="{{user.icon}}" alt="User icon">
    </div>
    <div class="col-6">
        <div>
            <h1>
                Hi {{user.username}}
            </h1>
        </div>
    </div>
</div>
<div>
    <h3>MY CLUBS</h3>
    <ul> {{#each club.clubname}}
        <li>
            {{this.genre}}
        </li>
        <li>
            {{this.currBook}}
        </li>
        {{/each}}
    </ul>
    <p>{{#each club.prevBooks}} | {{this}} {{/each}}</p>
</div>