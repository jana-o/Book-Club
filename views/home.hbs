<div>Test</div>
<div class="row">
    <div class="col-6">
        <img class="card-img-top" src="{{user.icon}}" alt="User icon">
    </div>
    <div class="col-6">
        <div>
            <h1>
                Hi {{user.username}}
            </h1>
        </div>
    </div>
</div>
<div>
    <h3>MY CLUBS</h3>
    <div>
        {{#each club}} {{> clubCard this}} {{/each}}
    </div>
    <p>{{#each club.prevBooks}} | {{this}} {{/each}}</p>
</div>